<?xml version='1.0' encoding='utf-8'?>
<!--
  Debug-only override of the launcher label, so the icon on the phone says which build is
  installed: this one shares its package name with the Play TWA and replaces it, and "did the
  install actually take?" is otherwise unanswerable from the launcher. Variant resources
  replace main ones, so nothing else in strings.xml needs repeating here.
-->
<resources>
    <string name="app_name">Shopping dev</string>
</resources>
